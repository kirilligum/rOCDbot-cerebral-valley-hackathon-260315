# ADR-003: Deterministic fallback path

- Decision: use `cache/critic_response.json` on timeout/schema faults.
- Rationale: preserve demo determinism and avoid model outages.
- Consequence: fallback responses are tagged with `source=cache` and explicit error code.
