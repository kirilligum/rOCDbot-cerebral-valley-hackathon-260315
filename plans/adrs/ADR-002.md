# ADR-002: Iterative completion cap

- Decision: use bounded loop execution with `NEBIUS_MAX_LOOP_STEPS` (default 3).
- Rationale: prevent unbounded execution while preserving closed-loop correction attempts.
- Consequence: completion is checked after each correction step.
