# ADR-001: Nebius text model path

- Decision: use `NEBIUS_TOKEN_FACTORY_TEXT_MODEL` for live critic calls.
- Rationale: deterministic and explicitly configurable at runtime.
- Consequence: live and cached dry paths must preserve this field.
